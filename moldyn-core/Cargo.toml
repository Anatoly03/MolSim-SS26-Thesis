[package]
name = "moldyn-core"
version = "0.1.0"
authors = ["Anatoly Weinstein"]
edition = "2024"
publish = false

[features]
nightly = []

[dependencies]
meval = { version = "0.2", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
